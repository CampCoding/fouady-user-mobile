import { colors } from './colors';
import { spacing } from './spacing';
import { typography, fonts, FontWeight } from './typography';

export { colors, spacing, typography, fonts };
export type { FontWeight };
export const theme = {
  colors,
  spacing,
  typography,
  fonts,
};

